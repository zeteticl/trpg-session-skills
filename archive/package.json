{
  "name": "trpg-session-archive",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-vault.mjs",
    "sync:hub": "node scripts/sync-vault.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.1.0"
  }
}
